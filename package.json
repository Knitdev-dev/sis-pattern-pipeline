{
  "name": "sis-pattern-pipeline",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx trigger.dev@latest dev",
    "deploy": "npx trigger.dev@latest deploy"
  },
  "dependencies": {
    "@trigger.dev/sdk": "^3.0.0",
    "resend": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
